print'o anti cheat podre foi removido'
--[[for i, v in pairs(getsenv(game:GetService'Chat':WaitForChild'ChatService')) do
    if type(v) == 'function' and islclosure(v) then
        hookfunction(v, function()
            return wait(9e9)
        end)
    end
end]]
